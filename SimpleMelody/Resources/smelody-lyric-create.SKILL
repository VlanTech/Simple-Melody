---
name: smelody-lyric-create
description: 当用户要求生成可导入 Simple Melody（macOS 原生歌词创作工具）歌词文件的 .smelody.txt 时调用此 Skill；或用户要求"按 smelody 格式写歌词"/"输出 .smelody.txt 格式"/"歌词要能导入 Simple Melody"等场景触发。
---

# smelody-lyric-create · 词炼成

> Simple Melody 歌词创作 Skill · 写出一份能被 Simple Melody 完美导入的 `.smelody.txt` 文件

## 〇、Skill 是什么

这是一个**给 AI Agent 用的 Skill**。当用户让你（AI）"写一首能导入 Simple Melody 的歌词"或"按 smelody 格式输出歌词"时：

1. 你（AI）**严格按照本 Skill 的所有规则**
2. 输出**一份完整的 `.smelody.txt` 内容**（纯文本，不带 markdown 代码块包裹）
3. 用户会把它保存成 `xxx.smelody.txt` 然后导入 Simple Melody

**本 Skill 的目标**：让你的输出**一次过导入**，不需要用户反复修改文件。

---

## 一、Simple Melody 软件背景（你必须先理解）

Simple Melody 是一款 macOS 原生歌词创作工具，结构：

- **左侧**：歌曲列表（支持文件夹 + 回收站 + 多选）
- **中间**：歌词段落编辑器（按类型分段：主歌 / 副歌 / 桥段……）
- **右侧**：灵感与设定（记录创作背景、动机、参考资料）

一首歌包含：
- **元信息**：标题、艺术家、专辑、BPM、调式、节拍、语言、标签
- **段落**：按类型分段（如 Intro / Verse / Chorus / Bridge……），每段有正文 + 笔记
- **灵感**：按类型分组（Inspiration / Setting / Background / Note），每个灵感是一个独立条目

**导入 `.smelody.txt` 时，Simple Melody 用文本解析恢复这些结构**。本 Skill 就是教你怎么写出能被这种解析器正确读取的文件。

---

## 二、整体文件结构（必须严格遵守）

```
==================================================     ← 50 个等号（顶部分隔线）
  Simple Melody · 歌词导出                              ← 标题行（可省略，建议保留）
==================================================     ← 50 个等号（顶部分隔线）

【Song Info】                                          ← 第 1 段头：歌曲元信息
Title: 歌名                                            ← key: value 格式
Artist: 艺术家
BPM: 90
Beat: 4/4
Languages: ja, zh
Tags: 流行, 抒情

【Lyrics】                                             ← 第 2 段头：歌词正文
                                                       ← 空行后开始歌词段落

[Verse 1]                                              ← 段落标记（半角方括号）
歌词内容第一行
歌词内容第二行

  段落笔记:                                            ← 段落笔记标记（中文）
    笔记内容（4 空格缩进）                               ← 4 空格缩进

[Chorus]
副歌内容

【Ideas & Settings】                                   ← 第 3 段头：灵感与设定

◆ Inspiration                                         ← 灵感类型标记（◆ + 英文名）
  • 第一条灵感                                          ← 条目标记（2 空格 + • + 1 空格）
  • 第二条灵感

◆ Setting
  • 设置描述

==================================================     ← 底部分隔线
# Simple Melody · Apple Silicon Native                  ← 页脚注释（# 开头，跳过解析）
==================================================
```

**3 个必要段头**：`【Song Info】` / `【Lyrics】` / `【Ideas & Settings】`
- `【Song Info】` 必须有（至少要有 `Title:`）
- `【Lyrics】` 和 `【Ideas & Settings】` 可选（如果不需要可省略）

---

## 三、段头识别规则（细节）

**段头必须用全角方括号 `【` `】` 包裹**，前后无空格。

```
【Song Info】       ← 正确
【 Lyrics 】        ← 错误：方括号内有空格
【lyrics】          ← 错误：全角 → 半角方括号（用【】不是 []）
[Lyrics]            ← 错误：半角方括号（这个用在段落标记，不是段头）
```

**段头会被解析器转小写后匹配**，所以 `【song info】` 和 `【Song Info】` 等价。但建议用规范的大小写。

**段头与下一个段头之间的所有内容都属于该段**。

**重要**：段头识别**只在每行的开头位置**生效。也就是说 `  【Lyrics】`（前面有空格）**不会被识别**为段头（但解析时会被 trim 后识别）。建议段头单独一行，前面无空格。

---

## 四、`【Song Info】` 段详解

### 4.1 整体格式

每行一条元信息，格式：`Key: Value`（英文冒号 `:`，冒号后必须有 1 个空格）。

```
【Song Info】
Title: 夏日漫步
Artist: 晨光乐团
BPM: 96
Key: G Major
Beat: 4/4
Languages: zh
Tags: 流行, 清新, 校园, 吉他
```

### 4.2 字段详解（逐个说清楚）

#### `Title:`（**必填**）
- **类型**：字符串
- **作用**：歌曲标题
- **示例**：`Title: 夏日漫步`
- **注意**：
  - 这是**唯一一个必填字段**。如果整个文件只有 `Title: xxx` 一行 + 一个段头 + 一个段落，仍然能导入
  - 标题**不能为空字符串**（写 `Title: ` 会被识别为空标题，解析器会报错或用 fallback）
  - 如果整个文件没有任何 `Title:` 行，且**所有段头都没匹配**，解析器会尝试从第一行非空文本推断标题
  - 标题中可以包含任何字符（包括 emoji、特殊符号、中日韩文字）

#### `Artist:`
- **类型**：字符串
- **作用**：艺术家 / 演唱者
- **示例**：`Artist: 晨光乐团`
- **注意**：
  - 如果文件里没有 `Artist:` 行，导入后艺术家字段为空字符串
  - 空字段在导入时会**跳过该行**（不会写空字符串进数据库）

#### `Album:`
- **类型**：字符串
- **作用**：专辑名
- **示例**：`Album: 城市印象`
- **注意**：同上，空则跳过

#### `BPM:`
- **类型**：整数（不要带小数点）
- **作用**：每分钟节拍数
- **示例**：`BPM: 96`
- **注意**：
  - **必须是整数**，写 `BPM: 96.5` 解析会失败（BPM 字段是 Int 类型）
  - 如果 `BPM:` 行存在但 value 不是整数，整首歌曲的 BPM 字段会被设为 nil
  - **BPM 本身没有上限**，但建议 40-200 之间（合理音乐范围）

#### `Key:`
- **类型**：字符串（自由文本）
- **作用**：调式
- **示例**：`Key: G Major` / `Key: A Minor` / `Key: C# Minor`
- **注意**：
  - **完全自由文本**，Simple Melody 不做调式合法性校验
  - 常见写法：`C Major` / `A Minor` / `F# Minor` / `Bb Major`
  - 也可以写中文：`Key: C 大调` / `Key: A 小调`（软件会原样保存）

#### `Beat:`
- **类型**：字符串（建议用标准预设）
- **作用**：节拍（如 4/4、3/4、6/8）
- **示例**：`Beat: 4/4`
- **标准预设**（推荐）：
  - `2/4`（2 拍子，华尔兹、波兰舞曲）
  - `3/4`（3 拍子，华尔兹）
  - `4/4`（4 拍子，流行、摇滚，**默认值**）
  - `6/8`（6 拍子，摇滚、慢摇滚）
  - `7/8`（7 拍子，爵士、前卫摇滚）
  - `9/8`（9 拍子，爵士、华尔兹变体）
  - `12/8`（12 拍子，4/4 慢摇滚、布鲁斯）
- **自定义**：也可以写 `5/4` `7/4` `11/8` 等，Simple Melody 接受任何 `X/Y` 格式字符串
- **如果文件没有 `Beat:` 行**：默认 `4/4`

#### `Languages:`
- **类型**：用 ISO code（**逗号分隔**）
- **作用**：歌曲语言
- **示例**：`Languages: zh, en` / `Languages: ja` / `Languages: ja, zh, en`
- **支持的 code**：
  - `ja`（日语 / Japanese）
  - `zh`（中文 / Chinese，包括简体和繁体）
  - `en`（英语 / English）
  - `ko`（韩语 / Korean）
  - `es`（西班牙语 / Spanish）
  - `fr`（法语 / French）
  - `de`（德语 / German）
  - `it`（意大利语 / Italian）
  - `pt`（葡萄牙语 / Portuguese）
- **分隔符**：逗号 `,` / 中点 `·` / 分号 `;` 都行（解析器全部支持），但**推荐用逗号**
- **额外容错**：除了 code，还可以用显示名（`中文` / `日语` / `English`），解析器会自动转换
- **如果文件没有 `Languages:` 行**：默认 `ja`（日语）—— 注意！这是个**坑**，如果你写的歌词是中文但忘了 `Languages:` 行，会被默认成日语

#### `Tags:`
- **类型**：用逗号分隔
- **作用**：标签（用于搜索/分类）
- **示例**：`Tags: 流行, 校园, 吉他, 清新`
- **注意**：
  - 标签是字符串数组，每个 tag 用逗号分隔
  - 前后空格会被自动去除
  - 空标签会被过滤
  - 数量不限

#### `Created:` / `Modified:`
- **类型**：日期字符串（解析器会尝试解析成 Date）
- **示例**：`Created: 2026-06-22 14:30` / `Modified: 2026-06-22 15:00`
- **注意**：
  - **解析器容忍多种格式**（长日期、短日期、ISO 等）
  - 如果格式无法识别，这两个字段会用导入时的当前时间
  - 你（AI）写歌词时**不需要主动写**这两个字段 —— 用户也不会关心
  - 写错了也不会让导入失败，只是不生效

### 4.3 字段顺序

**字段顺序不影响解析**，但建议按这个规范顺序写（参考 Simple Melody 实际导出的格式）：

```
Title:           ← 第 1 行
Artist:          ← 第 2 行
Album:           ← 第 3 行
BPM:             ← 第 4 行
Key:             ← 第 5 行
Beat:            ← 第 6 行（位于 Key 之后）
Languages:       ← 第 7 行
Tags:            ← 第 8 行
Created:         ← 第 9 行
Modified:        ← 第 10 行
```

### 4.4 未知字段

解析器遇到不认识的 key（如 `Year:` `Genre:` `Composer:`）**会跳过**这一行，不会报错，也不会污染其他字段。

---

## 五、作词实务：字数、格律、押韵、节奏（**最常被忽略**）

**为什么这一章很重要**：之前章节讲的是**格式**（怎么写文件、怎么写段落标记）—— 但格式正确不等于歌词**质量**好。如果 AI 不知道"唱得过来"是什么意思，写出来的歌词会：

- 一行 20+ 字 → 唱了 8 小节还没唱完，节奏全乱
- 一段只有 2-3 行短句 → 节奏空旷，必须加"啊""哦"等语气词凑数
- 押韵混乱 → 听感不和谐

这一章教 AI 写**真正能唱的歌词**。

### 5.0.1 字数控制（核心）

**每行字数建议**（中文 1 字 = 1 音节）：

| 风格 / 拍号 | 每行字数 | 备注 |
|---|---|---|
| 流行抒情（4/4 拍，BPM 60-100）| **6-10 字** | 大多数流行歌的甜蜜区 |
| 民谣（4/4 拍，BPM 80-120）| **5-9 字** | 略短更自然 |
| 摇滚（4/4 拍，BPM 120+）| **4-8 字** | 短句爆发力强 |
| 慢歌 / 抒情 ballad（4/4 拍，BPM < 70）| **4-7 字** | 拖腔有空间 |
| Hip-Hop / Rap | **8-16 字** | 节奏密度高，可长可短 |

**每段总字数建议**（4-8 行的整段）：

| 段落类型 | 字数范围 | 备注 |
|---|---|---|
| Verse 主歌 | **30-50 字** | 叙事铺陈，4-6 行 |
| Pre-Chorus 导歌 | **20-35 字** | 蓄势，2-4 行（比 Verse 短）|
| Chorus 副歌 | **40-60 字** | 情感爆发，4-6 行（最长）|
| Bridge 桥段 | **20-35 字** | 转折，2-4 行 |
| Outro 尾声 | **10-25 字** | 收尾，1-3 行（最短）|

**总字数参考**：一首 4 分钟的流行歌通常 200-350 字（中文）。

### 5.0.2 音节数与节奏匹配

4/4 拍下：**每行建议 8-12 音节**（含语气词"啊""哦""呀"等）。

- 1 个中文字 = 1 音节
- 1 个英文单词 ≈ 1-2 音节（视单词长度）
- 1 个空格 = 0.5 音节（停顿）

**为什么音节数重要**：4/4 拍每小节 4 拍，每拍约 1 音节。如果一行 16 音节，需要 4 小节才能唱完——但通常一行歌词对应 2-4 小节（4-8 拍），所以 8-12 音节最合适。

**例外**：
- Hook / 副歌高潮可以**短句重复**（如"来呀来呀来"）
- Bridge 可以**长句叙事**（增加层次感）
- Rap / 快速说唱可以**密集字句**（但要控制语速）

### 5.0.3 押韵模式

| 模式 | 示例 | 适用 |
|---|---|---|
| **ABAB** | 行 1 A / 行 2 B / 行 3 A / 行 4 B | **最常见**，流行歌首选 |
| **AABB** | 行 1 A / 行 2 A / 行 3 B / 行 4 B | 古风、民谣 |
| **ABBA** | 行 1 A / 行 2 B / 行 3 B / 行 4 A | 强调中间行（少见）|
| **AAAA** | 每行末字都押同韵 | 副歌高潮（如"爱爱爱爱"）|
| **自由韵** | 不押韵，靠节奏和语气 | 民谣、Hip-Hop、Rock |

**中文押韵规则**：
- 押韵位置：通常在**行末最后一个字**（如"路/睡/鼓/土"押 "u" 韵母）
- 韵脚字举例：`a/ia` 韵：花/家/霞/沙/妈/啦；`i/ei` 韵：你/里/期/提/机；`u/ou` 韵：路/图/书/土/鼓/住
- 副歌押韵要稳定，主歌可以稍微自由
- 关键句（**金句 / punchline**）必须押韵

### 5.0.4 段落功能与字数的关系

| 段落 | 字数偏少/偏多 | 为什么 |
|---|---|---|
| **Intro** | 极少（0-15 字）| 器乐为主，人声是片段 |
| **Verse** | 中等（30-50 字）| 叙事铺陈，需要空间 |
| **Pre-Chorus** | **偏少**（20-35 字）| 蓄势，短的铺垫 |
| **Chorus** | **偏多**（40-60 字）| 情感饱满，重复强化 |
| **Bridge** | 中等（20-35 字）| 转折，可长可短 |
| **Outro** | **极少**（10-25 字）| 收尾，留白 |

**反模式**：
- ❌ 副歌比主歌短（情感不够爆发）
- ❌ Pre-Chorus 比 Verse 长（蓄势过头）
- ❌ Outro 比 Verse 长（收尾拖沓）

### 5.0.5 唱不过来的常见错误

**错误 1：一行超长（"大长句综合症"）**

❌ 错误示例（4/4 拍流行歌）：
```
[Verse 1]
走过老街的青石板路看见午后的猫在打盹冰镇汽水冒着气泡童年就这样藏在心底
```

✅ 正确做法（拆成 4 行，每行 8-10 字）：
```
[Verse 1]
走过老街的青石板路
看见午后的猫在打盹
冰镇汽水冒着气泡
童年就这样藏在心底
```

**错误 2：长句套长句（"呼吸断点缺失"）**

❌ 错误：
```
[Verse 1]
走过老街看见午后猫在打盹冰镇汽水冒着气泡童年藏在心底没有人记得那些日子
```

✅ 正确（加标点/换行给呼吸点）：
```
[Verse 1]
走过老街，看见午后猫在打盹
冰镇汽水冒着气泡
童年藏在心底
没有人记得那些日子
```

**错误 3：每行密度不均**

❌ 错误（一行长一行短）：
```
[Verse 1]
走过老街的青石板路
夏
冰镇汽水冒着气泡童年
```

✅ 正确（每行字数相近，6-10 字）：
```
[Verse 1]
走过老街的青石板路
看见午后的猫在打盹
冰镇汽水冒着气泡
童年藏在心底
```

### 5.0.6 拖音的常见错误

**错误 1：段落太短**

❌ 错误（Verse 只有 2 行 8 字，4/4 拍 8 小节唱不完）：
```
[Verse 1]
走过老街
童年难忘
```

✅ 正确（4-6 行 30-50 字）：
```
[Verse 1]
走过老街的青石板路
看见午后的猫在打盹
冰镇汽水冒着气泡
童年就这样藏在心底
```

**错误 2：副歌重复字太多**

❌ 错误（副歌 3 行只有 6 个不同字）：
```
[Chorus]
夏天夏天
走走走
啊啊
```

✅ 正确（副歌 4-6 行 40-60 字，每行 8-10 字）：
```
[Chorus]
这个夏天我们一起走
走过每一条熟悉的路口
就算时光匆匆流走
记忆永远不会褪色
这段旋律就像风
吹过心间永不消失
```

**错误 3：Hook / 记忆点缺失**

副歌的**金句**（punchline）应该是最洗脑的那一句，必须**短而有力**：
```
[Chorus]
这个夏天我们一起走        ← 金句（10 字）
走过每一条熟悉的路口
就算时光匆匆流走
记忆永远不会褪色          ← 收尾（8 字）
这段旋律就像风
吹过心间永不消失
```

### 5.0.7 在段落笔记里记录这些作词提示

**段落笔记应该包含**作词实务信息，让下游消费者（人类或 AI）能理解这首歌的作词结构：

```
[Chorus]
歌词正文...

  段落笔记:
    副歌功能：情感爆发，"口号感"要强
    押韵：ABAB（走/口/走/色）
    每行字数：8-10 字
    音节密度：8-10 音节/行（4/4 拍）
    关键句（punchline）："这个夏天我们一起走"（金句）
    整体字数：约 50 字（4/4 拍 8 小节能唱完）
```

**段落笔记里建议包含的作词信息**：

| 信息 | 示例 |
|---|---|
| 押韵模式 | "ABAB 押韵" / "每行末字押 i" |
| 每行字数 | "8-10 字/行" |
| 段落总字数 | "约 30 字（4-6 行）" |
| 段落功能 | "主歌：叙事铺陈" / "副歌：情感爆发" |
| 关键句 | "副歌金句：'这个夏天我们一起走'" |
| 音节密度 | "4/4 拍 8-10 音节/行" |
| 节奏标记 | "8 小节 / 4 小节过渡" |

### 5.0.8 写歌词前的自检（生成前必查）

AI 生成歌词后、输出前，对照以下清单：

- [ ] 每行字数 6-10 字（4/4 拍流行）
- [ ] 每段总字数 30-60 字
- [ ] 没有超过 15 字的长行
- [ ] 副歌总字数 ≥ 主歌总字数
- [ ] 押韵模式与段落笔记说明一致
- [ ] 金句（punchline）短而有力（≤ 10 字）
- [ ] 段落笔记里写明了"每行字数""押韵""关键句"等作词信息
- [ ] Intro / Outro 字数极少（不要写成完整段落）
- [ ] Pre-Chorus 蓄势（≤ 35 字）< Verse 字数

---

## 六、`【Lyrics】` 段详解

### 6.1 段落类型（13 种预设）

Simple Melody 识别以下 13 种段落类型（**全部用英文**）：

| 英文名 | 中文 | 颜色 | 典型用法 |
|---|---|---|---|
| `Intro` | 前奏 | 灰色 | 歌曲开头的器乐/氛围段 |
| `Verse` | 主歌 | 蓝色 | 叙事段，铺陈故事 |
| `Pre-Chorus` | 导歌 | 紫色 | 副歌前的过渡段 |
| `Chorus` | 副歌 | 红色 | 情感高潮，重复出现 |
| `Post-Chorus` | 后副歌 | 橙色 | 副歌后的回落段 |
| `Bridge` | 桥段 | 绿色 | 转折段，引入新元素 |
| `Hook` | 钩子 | 橙色 | 洗脑段，记忆点 |
| `Refrain` | 叠歌 | 蓝色 | 重复短句 |
| `Interlude` | 间奏 | 灰色 | 纯器乐过渡 |
| `Solo` | 独奏 | 紫色 | 乐器独奏段 |
| `Outro` | 尾声 | 灰色 | 歌曲结尾 |
| `Coda` | 终曲 | 灰色 | 收尾段落 |
| `Custom` | 自定义 | 紫色 | 完全自定义 |

**所有段落类型都用英文**：即使歌词是中文/日文，**段落标记必须是英文**（如 `[Verse]` `[Chorus]`）。解析器**不会**匹配 `[主歌]` `[副歌]` 这种中文标记。

### 6.2 段落标记格式

段落标记 = `[英文类型名]` 或 `[英文类型名 数字]`（带序号）

```
[Verse]       ← 第 1 次主歌（序号省略）
[Verse 1]     ← 显式写 1
[Verse 2]     ← 第 2 次主歌
[Chorus]      ← 第 1 次副歌
[Chorus 2]    ← 第 2 次副歌
```

**关键规则**：
- 必须用**半角方括号** `[]`（不是全角 `【】`）
- 类型名是英文，**不区分大小写**（`[verse]` `[VERSE]` 都能识别，但推荐 `[Verse]`）
- 数字部分用空格分隔
- 段落标记**必须单独占一行**（不能跟正文同行）

### 6.3 段落正文

段落标记后的所有非空行都属于该段落的正文，直到：
1. 下一个段落标记行（`[xxx]`）
2. 段落笔记标记行（`段落笔记:` / `Notes:`）
3. 下一个段头行（`【xxx】`）

```
[Verse 1]
这是段落的第 1 行
这是段落的第 2 行
这是段落的第 3 行

[Chorus]
副歌第 1 行
副歌第 2 行
```

**段落正文可以多行**。解析器会把所有正文行用换行符合并成一个字符串。

**段落正文可以为空**（只有段落标记没有正文）：

```
[Intro]
（空段，用作纯器乐标识）

[Bridge]
```

**段落正文可以有空格行**（如果用空行作为段内分隔，需要小心 — 解析器会保留空行）：

```
[Verse 1]
第一段第一句
              ← 空行（保留）
第一段第二句
```

实际上解析器对空行的处理比较宽松：连续空行会被合并，单独空行会保留为段落正文的一部分。如果你想要段落笔记 / 新段落，应该用段落标记，而不是依赖空行。

### 6.4 段落笔记格式（最容易踩坑的部分）

**段落笔记的标识符**：使用 `段落笔记:` （中文）或 `Notes:` （英文，大小写不敏感）

```
[Verse 1]
歌词内容第一行

  段落笔记:                            ← 注意：前面有 2 个空格
    这是笔记的第一行                    ← 注意：前面有 4 个空格（缩进）
    这是笔记的第二行                    ← 同样 4 空格缩进
    这是包含多行的笔记                  ← 同上
```

**缩进规则（关键）**：

| 元素 | 缩进 | 示例 |
|---|---|---|
| `段落笔记:` 标记行 | **2 个空格** | `  段落笔记:` |
| 笔记内容行 | **4 个空格** | `    这是笔记` |

**为什么 2 空格 + 4 空格**？这是参考 Simple Melody 实际导出格式的设计。`段落笔记:` 行有 2 空格缩进（让它在视觉上"属于"上一段歌词但不顶格），具体内容有 4 空格缩进（清晰的层级关系）。

**AI 容易踩的坑**：

1. ❌ 忘记缩进：`段落笔记:` 写在最左边会被当成歌词的一部分
2. ❌ 缩进不一致：`段落笔记:` 用 4 空格而内容用 2 空格
3. ❌ 多段笔记串行：`段落笔记:` 写了多个，每个都各自独立（每个段落只能有一个笔记块）

### 6.5 段落嵌套 vs 段落重复

**常见错误**：用户想表达"重复的副歌"时，AI 可能写成嵌套结构。

错误示例 ❌：

```
[Verse 1]
主歌

[Chorus]
  [Chorus]
  副歌
```

正确示例 ✅：

```
[Verse 1]
主歌

[Chorus]
副歌内容

[Verse 2]
第二个主歌

[Chorus]
副歌内容（重复）
```

每个段落标记**必须是顶格或带正确缩进的独立行**，不能嵌套在其他段落里。

---

## 七、`【Ideas & Settings】` 段详解（最容易误读的部分）

### 7.1 灵感类型（4 种预设，全部用英文）

| 英文名 | 中文 | 图标 | 用途 |
|---|---|---|---|
| `Inspiration` | 灵感 | 💡 | 创作灵感、动机、突发想法 |
| `Setting` | 设定 | 📄 | 故事背景、场景、人物设定 |
| `Background` | 背景 | 📖 | 创作背景、参考来源、相关故事 |
| `Note` | 备注 | 📝 | 编曲建议、技术备注、待办 |

**类型标记格式**：`◆ 类型英文名`（**全角菱形 `◆` + 1 空格 + 英文名**）

```
◆ Inspiration         ← 灵感类型标记
◆ Setting             ← 设定类型标记
◆ Background          ← 背景类型标记
◆ Note                ← 备注类型标记
```

### 7.2 灵感条目格式（**最容易 AI 误读**）

**条目标记**：`2 空格 + • + 1 空格 + 内容`

```
◆ Inspiration
  • 这是第一条灵感                          ← 2 空格 + • + 1 空格 + 内容
  • 这是第二条独立的灵感                      ← 同上
  • 这是第三条，每个 • 是一个独立的完整想法    ← 重要！
```

**重要原则（必读）**：

> **每个 `•` 是 ONE 个独立的完整想法 / 条目**，不是一段话里的多个 `•`。

AI 容易犯的错误：

❌ **错误 1**：把一段长文里的所有 `•` 都塞进一个灵感条目

```
◆ Inspiration
  • 创作灵感来自 2019 年夏天的校园生活
    • 那年夏天学校组织了一次徒步旅行          ← 错误：嵌套 • 不是新条目
    • 途经老街、操场和小山
    • 在山顶大家合唱了一首老歌
```

应该改成：

```
◆ Inspiration
  • 创作灵感来自 2019 年夏天的校园生活        ← 一条完整想法
  • 那年夏天学校组织了一次徒步旅行，途经老街、操场和小山  ← 一条完整想法
  • 在山顶大家合唱了一首老歌，记忆深刻        ← 一条完整想法
```

❌ **错误 2**：把同一条想法拆成多个 `•` 行（因为看起来"工整"）

```
◆ Inspiration
  • 创作灵感来自                              ← 拆开成多行是错的
  • 2019 年夏天
  • 的校园生活
```

应该改成：

```
◆ Inspiration
  • 创作灵感来自 2019 年夏天的校园生活        ← 一条完整想法
```

✅ **正确格式**：每条想法**用完整的一句话或一段话描述**，独立成行，每个想法一条 `•` 行。

**总结**：
- **多个 `•` 是平行的多个独立想法**（不同想法）
- **不是嵌套 / 不是段落中的多个项目符号**
- **不要因为一个想法"长"就拆成多个 `•`** —— 一个完整想法就一条 `•`

### 7.3 灵感条目数量建议

- **Inspiration**（灵感）：3-8 条
- **Setting**（设定）：2-5 条
- **Background**（背景）：2-5 条
- **Note**（备注）：2-5 条

总数 **10-20 条左右**比较合适。不要太少（信息不足），也不要太多（过于冗长）。

### 7.4 灵感内容写作建议

每条灵感条目应该是**一句话或一段话**，内容具体、信息密度高：

✅ **好的灵感条目**：
```
• 创作灵感来自 2019 年夏天的校园生活
• 副歌旋律最初是在操场边哼出来的
• 编曲建议：木吉他 + 轻鼓刷 + 贝斯 + 偶尔的钢琴点缀
```

❌ **差的灵感条目**（太短、太抽象）：
```
• 灵感很好
• 副歌好
• 编曲要用心
```

---

## 八、注释和分隔线

### 8.1 顶部 / 底部分隔线

**50 个等号 `=` 组成的水平线**，作为文件顶部和底部的视觉分隔。

```
==================================================
  Simple Melody · 歌词导出
==================================================
```

**底部同样**：
```
==================================================
# Simple Melody · Apple Silicon Native
==================================================
```

### 8.2 行内分隔线

如果文件中出现**整行都是 `=` 的情况**（如 `==`、`===` 等），解析器会**整行跳过**。

所以你也可以用任意长度的 `=` 作为段内视觉分隔（不影响解析）。

### 8.3 注释行（`#` 开头）

**任何以 `#` 开头的行都会被解析器跳过**，不参与解析。

通常用作页脚信息：

```
==================================================
# Simple Melody · Apple Silicon Native
==================================================
```

你也可以在文件任何位置添加注释行（如 `# 这首歌是为毕业季写的`），这些行会被忽略，不影响歌曲内容。

---

## 九、完整格式示例（必看）

### 示例 1：中文流行歌《夏日漫步》

```txt
==================================================
  Simple Melody · 歌词导出
==================================================

【Song Info】
Title: 夏日漫步
Artist: 晨光乐团
Album: 城市印象
BPM: 96
Key: G Major
Beat: 4/4
Languages: zh
Tags: 流行, 清新, 校园, 吉他

【Lyrics】

[Intro]

  段落笔记:
    用原声吉他开场，营造夏日午后的慵懒氛围
    8 小节，前 4 小节扫弦铺垫，后 4 小节过渡到主歌

夏日的天空
蓝色像被洗过一样
蝉鸣声从树梢传来
微风拂过我的脸庞

[Verse 1]
走过老街的青石板路
看见午后的猫在打盹
冰镇汽水冒着气泡
童年就这样藏在心底

  段落笔记:
    第一段是铺垫，描述夏日街景
    押韵：路/睡/鼓/土，ABAB 句式
    情绪：温暖、怀念

[Pre-Chorus]
阳光斜斜地照进来
照亮了每一个角落
那些被遗忘的梦想
又开始在心里闪烁

[Chorus]
这个夏天我们一起走
走过每一条熟悉的路口
就算时光匆匆流走
记忆永远不会褪色
这段旋律就像风
吹过心间永不消失

  段落笔记:
    副歌是情感高潮，要写得强烈、有感染力
    适合配合乐队齐奏 + 鼓点

[Post-Chorus]

  段落笔记:
    用哼唱（humming）过渡

[Verse 2]
操场边的老槐树下
有人在弹着吉他
你笑起来的模样
比阳光还要灿烂

  段落笔记:
    第二段加入具体的人物

[Chorus]
这个夏天我们一起走
走过每一条熟悉的路口
就算时光匆匆流走
记忆永远不会褪色
这段旋律就像风
吹过心间永不消失

[Bridge]
如果有一天我们都老去
还有这首歌陪伴着你
那些夏天那些青春
永远不会忘记

  段落笔记:
    Bridge 是情感转折
    适合独唱 + 钢琴伴奏

[Chorus]
这个夏天我们一起走
走过每一条熟悉的路口
就算时光匆匆流走
记忆永远不会褪色
这段旋律就像风
吹间永不消失

[Outro]

  段落笔记:
    用同样的吉他扫弦收尾
    渐弱至 fade out

==================================================
【Ideas & Settings】

◆ Background
  • 创作灵感来自 2019 年夏天的校园生活
  • 那年夏天学校组织了一次徒步旅行，途经老街、操场和小山
  • 当年在山顶大家合唱了一首老歌，记忆深刻

◆ Setting
  • 时间：盛夏午后，蝉鸣阵阵
  • 地点：南方某座小城的校园周边
  • 氛围：慵懒、温暖、充满回忆

◆ Inspiration
  • "这个夏天我们一起走" 来自徒步时同学们的口号
  • 副歌旋律最初是在操场边哼出来的
  • 吉他 riff 受《那些花儿》启发，但节奏更快一些

◆ Note
  • 编曲建议：木吉他 + 轻鼓刷 + 贝斯 + 偶尔的钢琴点缀
  • 主歌以人声为主，副歌加入和声层
  • Bridge 可以降半调处理增加张力

==================================================
# Simple Melody · Apple Silicon Native
==================================================
```

### 示例 2：日文流行歌《桜の約束》（极简）

```txt
==================================================
  Simple Melody · 歌词导出
==================================================

【Song Info】
Title: 桜の約束
Artist: 春風
BPM: 78
Key: A Minor
Beat: 4/4
Languages: ja, zh
Tags: J-POP, 抒情, 钢琴, 春季

【Lyrics】

[Intro]

  段落笔记:
    钢琴前奏，4 小节

桜が舞う街角で
君と出逢った春の日

[Verse 1]
風に乗って届いた
君の笑顔が今も

  段落笔记:
    押韵：届いた / 今も

[Chorus]
桜の約束
永遠に続くように

[Outro]
また来年 会おう

==================================================
# Simple Melody · Apple Silicon Native
==================================================
```

---

## 十、常见错误 & 自检清单

### 10.1 致命错误（导入会失败）

| 错误 | 症状 | 修正 |
|---|---|---|
| 没有 `Title:` | 解析器找不到标题字段 | 必须写 `Title: 歌名` |
| 用中文段头 `【歌词】` | 解析器不识别 | 必须用 `【Lyrics】` |
| 段落标记用全角 `[Verse]` | 半角方括号 `[Verse]` 才是段落标记 | 改成半角 `[]` |
| 段落标记用中文 `[主歌]` | 解析器不识别英文以外的标记 | 改成 `[Verse]` `[Chorus]` 等 |
| Languages 用全名 | 解析器找不到 code | 用 `zh` `ja` `en` 等 ISO code |
| `段落笔记:` 没缩进 | 被当成歌词 | 加 2 空格缩进 |

### 10.2 非致命错误（导入会成功但显示异常）

| 错误 | 症状 | 修正 |
|---|---|---|
| 字段顺序错乱 | 软件显示按字段顺序展示 | 按推荐顺序：Title / Artist / Album / BPM / Key / Beat / Languages / Tags |
| 段落笔记内容没 4 空格缩进 | 笔记内容被当成歌词 | 加 4 空格缩进 |
| `•` 写成 `-` 或 `*` | 解析器不识别 | 必须用 `•`（Unicode bullet U+2022） |
| 灵感条目嵌套 `•` | 嵌套被忽略 | 每个 `•` 是独立的，不要嵌套 |
| 灵感 `◆` 写成 `#` 或 `·` | 解析器不识别 | 必须用 `◆`（Unicode U+25C6） |
| Beat 用不规范格式 | 字段被原样保存但不识别 | 推荐 `4/4` `3/4` `6/8` 等 |
| BPM 写成小数 | 整数字段变 nil | 用整数 `BPM: 90` |

### 10.3 视觉一致性建议

| 建议 | 说明 |
|---|---|
| 段间空行 | 每个段落标记前留 1 空行（视觉清晰，解析器允许） |
| `段落笔记:` 前空行 | 让笔记标记更突出（但 `段落笔记:` 不能有前导空行才能被解析为该段的笔记） |
| 顶部 / 底部分隔线 | 用 50 个 `=` |
| 灵感类型标记后空行 | 每个 `◆ 类型` 后空一行，然后开始 `  • 条目` |

### 10.4 自检清单（每次输出前核对）

**生成歌词文件后，AI 必须自检以下项目**：

- [ ] 顶部有 50 个 `=` 分隔线（可选但建议）
- [ ] 至少有 `【Song Info】` 段头
- [ ] `Title:` 字段存在且非空
- [ ] `BPM:` 是整数（如果写了的话）
- [ ] `Beat:` 用 `X/Y` 格式（如果写了的话）
- [ ] `Languages:` 用 ISO code（`zh` `ja` `en` 等）
- [ ] 所有段落标记是英文（`[Verse]` `[Chorus]` 等）
- [ ] 段落标记用半角方括号 `[]`
- [ ] 段落标记单独占一行
- [ ] 段落正文跟段落标记在同一行之后的下一行起
- [ ] `段落笔记:` 前面有 2 空格缩进
- [ ] 段落笔记内容前面有 4 空格缩进
- [ ] 灵感类型用 `◆ 英文名`
- [ ] 灵感条目用 `  • 内容`
- [ ] 每个 `•` 是一个完整的独立想法（不是嵌套）
- [ ] 注释行用 `#` 开头
- [ ] 行内 `=` 分隔线不影响解析

---

## 十一、输出格式要求（AI 如何呈现结果）

当你（AI）按照本 Skill 写歌词时：

1. **输出完整文件内容**（纯文本）
2. **不要用 markdown 代码块包裹**（用户会保存为 `.smelody.txt`，代码块标记 ` ``` ` 会成为文件内容的一部分，导致解析失败）
3. **不要添加额外解释**（如"以下是歌词："这种引导语）
4. **直接输出歌词文件内容**

错误示例 ❌：

```
以下是按 smelody 格式生成的歌词：

```
==================================================
  Simple Melody · 歌词导出
==================================================
...
```

应该输出：

```
==================================================
  Simple Melody · 歌词导出
==================================================
...
```

**（注意：纯文本开头，不带任何 markdown 标记）**

---

## 十二、用户交互模式

当用户输入类似以下请求时，使用本 Skill：

- "写一首能导入 Simple Melody 的歌词"
- "按 smelody 格式输出这首歌"
- "用 .smelody.txt 格式写一份歌词"
- "给我一首 Simple Melody 格式的歌词"
- "歌词要做成 smelody 格式"

如果用户没有指定格式要求，但要求"写歌词"且提到 Simple Melody 软件，使用本 Skill。

如果用户要求其他格式（如 Markdown 歌词稿、纯歌词文本不要求可导入），**不要**用本 Skill —— 用普通方式写即可。

---

## 十三、Skill 维护

本 Skill 基于 Simple Melody v1.7.5 Gamma 的导入逻辑编写。如果 Simple Melody 后续版本更新了导入格式，本 Skill 也需要同步更新。

**验证方法**：
1. AI 按本 Skill 生成一份歌词
2. 保存为 `xxx.smelody.txt`
3. 在 Simple Melody 中导入
4. 如果导入后所有字段、段落、笔记、灵感都正确显示 = 本 Skill 工作正常
5. 如果有字段错误或段落错乱 = 需要更新本 Skill 的对应章节